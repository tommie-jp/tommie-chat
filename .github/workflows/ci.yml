name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - uses: actions/setup-go@v6
        with:
          go-version-file: nakama/go_src/go.mod
          cache-dependency-path: nakama/go_src/go.sum
      - run: npm ci
      - run: npm run check
      - run: npm run build
      - name: Go unit tests (nakama plugin)
        working-directory: nakama/go_src
        run: go test ./...
      - name: Shell script syntax check
        run: |
          failed=0
          for f in $(find . -name '*.sh' -not -path '*/node_modules/*'); do
            if ! bash -n "$f"; then
              echo "❌ $f"
              failed=1
            fi
          done
          exit $failed
      - name: Upload dist
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/
          retention-days: 7
