{
    "name": "VE 直後の PI にも PO を返す (状態遷移無し)",
    "section": "§6.2 #3, #4",
    "required": true,
    "timeout_ms": 1000,
    "steps": [
        {
            "tx": "VE"
        },
        {
            "rx_regex": "^VE[0-9]{2}.{1,16}$"
        },
        {
            "tx": "PI"
        },
        {
            "rx": "PO"
        }
    ]
}
