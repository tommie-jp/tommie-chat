{
    "name": "小文字 ve は §4 違反で ER02 応答",
    "section": "§4, §4.1, §6.3",
    "required": true,
    "timeout_ms": 500,
    "_note": "v0.1: コマンドは大文字のみ受理。小文字は ER02 (Lowercase command) で拒否。",
    "steps": [
        { "tx": "ve" },
        { "rx_regex": "^ER02( .*)?$" }
    ]
}
