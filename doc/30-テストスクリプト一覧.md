# テストスクリプト一覧

テスト用シェルスクリプトの一覧とオプション。
全スクリプトは `test/` ディレクトリに配置。

前提: `cd nakama && docker compose up -d` でサーバ起動済みであること。

## 目次

- [リモートホストへのテスト](#リモートホストへのテスト)
- [レポートシンボリックリンク一覧](#レポートシンボリックリンク一覧)
- 一括実行スクリプト
  - [doAll.sh — 全テスト一括実行](#doallsh--全テスト一括実行)
  - [doNight.sh — 夜間長時間バッチテスト](#donightsh--夜間長時間バッチテスト)
- 個別テストスクリプト
  - [doTest-concurrent-login.sh — 同時接続テスト](#dotest-concurrent-loginsh--同時接続テスト)
  - [doTest-sustain.sh — 持続接続テスト](#dotest-sustainsh--持続接続テスト)
  - [doTest-snd-rcv.sh — 送受信整合性テスト](#dotest-snd-rcvsh--送受信整合性テスト)
  - [doTest-player-list.sh — プレイヤーリスト通知テスト](#dotest-player-listsh--プレイヤーリスト通知テスト)
  - [doTest-ccu-db.sh — 同接履歴 DB永続化テスト](#dotest-ccu-dbsh--同接履歴-db永続化テスト)
  - [doTest-max-players.sh — 最大同時接続人数探索](#dotest-max-playerssh--最大同時接続人数探索)
  - [doTest-reconnect.sh — 再接続テスト](#dotest-reconnectsh--再接続テスト)
  - [doTest-security.sh — セキュリティテスト](#dotest-securitysh--セキュリティテスト)
  - [doTest-ping.sh — サーバ疎通テスト](#dotest-pingsh--サーバ疎通テスト)
  - [doTest-ping-remote.sh — リモート疎通テスト](#dotest-ping-remotesh--リモート疎通テスト)
  - [doTest-https.sh — HTTPS デプロイ確認テスト](#dotest-httpssh--https-デプロイ確認テスト)
- デプロイテスト
  - [doDeployTest.sh — デプロイテスト](#dodeploytestsh--デプロイテスト)
  - [doDeployTest-curl.sh — デプロイテスト（curl版）](#dodeploytest-curlsh--デプロイテストcurl版)
- ユーティリティスクリプト
  - [doLint.sh — 文法チェック](#dolintsh--文法チェック)
  - [doSetupTest.sh — ゼロセットアップ検証](#dosetuptestsh--ゼロセットアップ検証)
  - [doCleanLog.sh — ログ削除](#docleanlogsh--ログ削除)
  - [doClearGround.sh — 地面データ初期化](#docleargroundsh--地面データ初期化)
  - [doClearUsers.sh — ユーザーデータ初期化](#doclearuserssh--ユーザーデータ初期化)
  - [doSeedGround.sh — 初期地面データ投入](#doseedgroundsh--初期地面データ投入)

## リモートホストへのテスト

全テストスクリプトは `--host` / `--port` オプションまたは `NAKAMA_HOST` / `NAKAMA_PORT` 環境変数でリモートサーバへのテストに対応しています。
リモートホスト指定時は Go ビルド・サーバ再起動・Docker ログ収集が自動スキップされます。
テスト間のサーバ再起動もスキップされるため、前テストのプレイヤーが残った状態で次テストが実行されます。
各テストは固有のユーザー名を使うため大半のケースで問題ありませんが、厳密なテスト分離が必要な場合はローカルで実行してください。

| オプション / 環境変数 | 説明 | デフォルト |
| --- | --- | --- |
| `--host HOST` / `NAKAMA_HOST` | 接続先ホスト | `127.0.0.1` |
| `--port PORT` / `NAKAMA_PORT` | Nakama API ポート | `7350` |

```bash
# --host / --port オプション
./test/doAll.sh --host mmo.tommie.jp --port 443
./test/doTest-ping.sh --host mmo.tommie.jp

# 環境変数
NAKAMA_HOST=mmo.tommie.jp NAKAMA_PORT=443 ./test/doAll.sh

# 個別スクリプト
./test/doTest-snd-rcv.sh --host mmo.tommie.jp -n 100
./test/doTest-sustain.sh --host mmo.tommie.jp --port 443 -n 50 -t 60

# doTest-security.sh は第1引数でリモートサーバ名を指定（常に HTTPS/443）
./test/doTest-security.sh mmo.tommie.jp
```

`.test.ts` ファイルも `NAKAMA_HOST` / `NAKAMA_PORT` 環境変数に対応しているため、
vitest を直接実行する場合は環境変数で指定できます:

```bash
NAKAMA_HOST=mmo.tommie.jp NAKAMA_PORT=443 npx vitest run test/nakama-concurrent.test.ts
```

## レポートシンボリックリンク一覧

`test/log/` 内の最新レポートへのシンボリックリンク:

| リンク名 | 対象テスト |
| --- | --- |
| [00-all.md](../test/log/00-all.md) | doAll.sh 全テスト一括 |
| [01-concurrent.md](../test/log/01-concurrent.md) | 同時接続テスト |
| [02-sustain.md](../test/log/02-sustain.md) | 持続接続テスト |
| [03-ccu-db.md](../test/log/03-ccu-db.md) | 同接履歴 DB永続化テスト |
| [04-snd-rcv.md](../test/log/04-snd-rcv.md) | 送受信整合性テスト |
| [05-max-players.md](../test/log/05-max-players.md) | 最大同時接続人数探索 |
| [05-player-list.md](../test/log/05-player-list.md) | プレイヤーリスト通知テスト |
| [06-lint.md](../test/log/06-lint.md) | 文法チェック |

---

## 一括実行スクリプト

### `doAll.sh` — 全テスト一括実行

5つのテストスクリプトを順番に実行する。人数を段階的に増やして繰り返し実行も可能。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `-n N` | N人で1回実行 | 各スクリプトの既定値 |
| `--counts C` | カンマ区切り人数リストで順に実行（失敗で停止） | — |
| `-s S` | S人から段階実行（失敗で停止） | — |
| `--step D` | `-s` と併用。D人ずつ増加 | 100,500,1000,1500,2000,3000... |
| `--host HOST` | 接続先ホスト | `NAKAMA_HOST` or `127.0.0.1` |
| `--port PORT` | Nakama API ポート | `NAKAMA_PORT` or `7350` |
| `-v` | 全ログ出力（間引きなし） | off |

**実行テスト:**

| # | スクリプト | 内容 |
| --: | --- | --- |
| 1 | doTest-concurrent-login.sh | 同時接続テスト |
| 2 | doTest-sustain.sh | 持続接続テスト |
| 3 | doTest-snd-rcv.sh | 送受信整合性テスト |
| 4 | doTest-player-list.sh | プレイヤーリスト通知テスト |
| 5 | doTest-ccu-db.sh | 同接履歴 DB永続化テスト |

**使用例:**

```bash
./test/doAll.sh                          # デフォルト人数で1回実行
./test/doAll.sh -n 2000                  # 2000人で1回実行
./test/doAll.sh --counts 100,500,1000,2000  # 段階実行（失敗で停止）
./test/doAll.sh -s 100 --step 100        # 100→200→300...と増加
./test/doAll.sh -s 100                   # 100→500→1000→1500→2000...
```

**ログ出力先:**

| ファイル | 内容 |
| --- | --- |
| `test/log/doAll-<テスト名>-YYYYMMDD-HHMMSS.log` | 各テストの全出力（ANSI除去済み） |
| `test/log/all-YYYYMMDD-HHMMSS.md` | Markdownレポート |
| `test/log/00-all.md` | 最新レポートへのシンボリックリンク |

---

### `doNight.sh` — 夜間長時間バッチテスト

doAll.sh を繰り返し実行し、長時間安定稼働を検証する。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `--1h` | 1時間コース: doAll.sh x 25回ループ | ✅ |
| `--4h` | 4時間コース: doAll.sh x 25回 + ccu-db --5m x 3回 | — |
| `--8h` | 8時間コース: 上記 + ccu-db --1h x 1回 | — |

---

## 個別テストスクリプト

### `doTest-concurrent-login.sh` — 同時接続テスト

複数人が同時にログインし、移動操作のパフォーマンスを計測する。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `-n N` | N人テストのみ実行 | 1, 10, 100, 1000, 2000人 |
| `--host HOST` | 接続先ホスト | `NAKAMA_HOST` or `127.0.0.1` |
| `--port PORT` | Nakama API ポート | `NAKAMA_PORT` or `7350` |

**ログ出力先:**

| ファイル | 内容 |
| --- | --- |
| `test/log/concurrent-login-YYYYMMDD-HHMMSS.md` | Markdownレポート |
| `test/log/01-concurrent.md` | 最新レポートへのシンボリックリンク |

---

### `doTest-sustain.sh` — 持続接続テスト

N人が一定時間接続を維持し、リコネクト回数・移動成功率・エラー率を検証する。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `-n N` | 接続人数 | 100 |
| `-t SEC` | テスト最大時間（秒） | 90 |
| `--host HOST` | 接続先ホスト | `NAKAMA_HOST` or `127.0.0.1` |
| `--port PORT` | Nakama API ポート | `NAKAMA_PORT` or `7350` |

**ログ出力先:**

| ファイル | 内容 |
| --- | --- |
| `test/log/sustain-YYYYMMDD-HHMMSS.md` | Markdownレポート |
| `test/log/02-sustain.md` | 最新レポートへのシンボリックリンク |

---

### `doTest-snd-rcv.sh` — 送受信整合性テスト

サーバログとクライアントログを並行取得し、各 snd/rcv ペアの整合性を確認する。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `-n N` | N人テストのみ実行 (1\|2\|3\|10\|100\|1000\|2000) | 全フェーズ |
| `-r R` | 秒あたりのログイン数 | 40 |
| `-t T` | テストタイムアウト（秒） | 人数に応じて自動 |
| `--host HOST` | 接続先ホスト | `NAKAMA_HOST` or `127.0.0.1` |
| `--port PORT` | Nakama API ポート | `NAKAMA_PORT` or `7350` |
| `--1000` | 1000人テストをデフォルト実行に追加 | off |

**テストフェーズ:**

| Phase | 内容 | 検証項目 |
| ---: | --- | --- |
| 1 | 1人ログイン | initPos, AOI_UPDATE, syncChunks, getServerInfo |
| 2 | 2人ログイン | AOI_ENTER 相互受信 |
| 3 | setBlock | ブロック設置・ブロードキャスト |
| 4 | AOI_LEAVE | 切断時の AOI_LEAVE 通知 |
| 5 | opMoveTarget | 移動の送受信 |
| 6 | opAvatarChange | アバター変更の送受信 |
| 7 | 3人ログイン | 複数人 AOI_ENTER |
| 8 | 10人ログイン | 複数人 AOI_ENTER |
| 9 | 100人ログイン | 大人数 AOI_ENTER |
| 10 | 1000人ログイン | 大規模ログイン整合性 |
| 11 | 2000人ログイン | 大規模ログイン整合性 |

**ログ出力先:**

| ファイル | 内容 |
| --- | --- |
| `test/log/doTest-snd-rcv-*-server.log` | 各フェーズのサーバログ |
| `test/log/doTest-snd-rcv-*-client.log` | 各フェーズのクライアントログ |
| `test/log/snd-rcv-YYYYMMDD-HHMMSS.md` | Markdownレポート |
| `test/log/04-snd-rcv.md` | 最新レポートへのシンボリックリンク |

---

### `doTest-player-list.sh` — プレイヤーリスト通知テスト

プロフィール情報（displayName, textureUrl, loginTime）がサーバから正しく取得できるかを検証する。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `-n N` | N人テストのみ実行 | 1, 10, 100, 1000, 2000人 |
| `-r R` | 秒あたりのログイン数 | 40 |
| `-t T` | テストタイムアウト（秒） | 人数に応じて自動 |
| `--host HOST` | 接続先ホスト | `NAKAMA_HOST` or `127.0.0.1` |
| `--port PORT` | Nakama API ポート | `NAKAMA_PORT` or `7350` |

**検証項目:** プロフィール通知、表示名変更通知、不正sessionId要求、テクスチャ変更反映、途中参加プロフィール取得

**ログ出力先:**

| ファイル | 内容 |
| --- | --- |
| `test/log/player-list-YYYYMMDD-HHMMSS.md` | Markdownレポート |
| `test/log/05-player-list.md` | 最新レポートへのシンボリックリンク |

---

### `doTest-ccu-db.sh` — 同接履歴 DB永続化テスト

同接数 (CCU) の DB 保存・再起動後の復元を検証する。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `--1m` | 1分: 1sサンプリング + RPC疎通 | ✅ |
| `--5m` | 5分: 1mフラッシュ + 再起動 + 履歴復元 | — |
| `--30m` | 30分: 複数回再起動で履歴累積 | — |
| `--1h` | 1時間: 長時間安定稼働 + レンジ整合性 | — |
| `--host HOST` | 接続先ホスト | `NAKAMA_HOST` or `127.0.0.1` |
| `--port PORT` | Nakama API ポート | `NAKAMA_PORT` or `7350` |

---

### `doTest-max-players.sh` — 最大同時接続人数探索

指定人数から増加幅ずつ増やし、失敗するまでテストを繰り返す。各人数で snd-rcv と player-list の2テストを実行し、両方PASSで合格。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `-s S` | 開始人数 | 100 |
| `--step D` | 増加幅 | 10 |
| `-r R` | 秒あたりのログイン数 | 40 |
| `--trials N` | 試行回数（過半数PASSで合格） | 1 |
| `--host HOST` | 接続先ホスト | `NAKAMA_HOST` or `127.0.0.1` |
| `--port PORT` | Nakama API ポート | `NAKAMA_PORT` or `7350` |
| `-v` | 全ログ出力 | off |

**ログ出力先:**

| ファイル | 内容 |
| --- | --- |
| `test/log/max-players-YYYYMMDD-HHMMSS.md` | Markdownレポート |
| `test/log/05-max-players.md` | 最新レポートへのシンボリックリンク |

---

### `doTest-reconnect.sh` — 再接続テスト

WebSocket切断→再接続後の状態復元を検証する。
2人のプレイヤーで切断→再接続を行い、AOI_ENTER・プロフィール・セッション管理が正しく動作するかを確認する。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `-h` | ヘルプ表示 | — |

**検証項目:**

| # | 内容 |
| --: | --- |
| 1 | 切断→再接続で新sessionIdを取得 |
| 2 | 再接続後に他プレイヤーのAOI_ENTERを受信 |
| 3 | 再接続後に他プレイヤーが新セッションのAOI_ENTERを受信 |
| 4 | 再接続後にプロフィール取得が正常動作（自分・他プレイヤー双方向） |
| 5 | 古いsessionIdでプロフィール取得すると空で返る |
| 6 | 3回連続再接続しても正常動作 |

**手動テスト（ブラウザ DevTools）:**

コンソールから `game.nakama.socket.disconnect()` を実行すると即座にWebSocketが切断され、
RETRY→再接続の一連のフローをテストできる。

```text
確認手順:
  1. ブラウザでログイン（別端末で他プレイヤーも接続しておく）
  2. DevTools Console で game.nakama.socket.disconnect() を実行
  3. 右上バッジが RETRY（黄色）になることを確認
  4. 1秒後に ON（緑）に復帰することを確認
  5. 同接数・プレイヤーリストが正しいことを確認
  6. 他プレイヤーのアバターが再表示されることを確認
```

> DevTools の Network → Offline でもテスト可能だが、WebSocketハートビートタイムアウト（60秒）
> まで切断が検出されないため、`game.nakama.socket.disconnect()` の方が即座にテストできる。

---

### `doTest-security.sh` — セキュリティテスト

XSS サニタイズ、nginx `/s3/` アクセス制限、RPC レート制限を検証する。

| 引数 | 説明 | デフォルト |
| --- | --- | --- |
| 第1引数 | リモートサーバ名 | なし（ローカル `127.0.0.1:7350`） |

**検証項目:**

| # | 内容 |
| --: | --- |
| 1 | 表示名の HTML エスケープ（`<script>` タグ） |
| 2 | 表示名の属性エスケープ（`onerror` ハンドラ） |
| 3 | ワールド名の HTML エスケープ |
| 4 | nameColor バリデーション関数の存在確認 |
| 5 | 本番設定で `ENABLE_TEST_RPC=false` が明示されている |
| 6 | `deleteUsers` RPC が実行不可（dev: 有効確認、本番: 無効確認） |
| 7 | `/s3/avatars/` GET が通る |
| 8 | `/s3/avatars/` PUT が拒否される |
| 9 | `/s3/uploads/` がブロックされる |
| 10 | `/s3/` ルートがブロックされる |
| 11 | `X-Content-Type-Options: nosniff`（本番 nginx のみ） |
| 12 | `X-Frame-Options: DENY`（本番 nginx のみ） |
| 13 | `Referrer-Policy` ヘッダーが存在する（本番 nginx のみ） |
| 14 | `Content-Security-Policy` ヘッダーが存在する（本番 nginx のみ） |
| 15 | 正常 Origin で `/v2/` が許可される（本番 nginx のみ） |
| 16 | Origin なしで `/v2/` が 403（本番 nginx のみ） |
| 17 | 不正 Origin で `/v2/` が 403（本番 nginx のみ） |
| 18 | 長すぎる表示名（31文字）がエラー |
| 19 | 表示名の上限ちょうど（30文字）は成功 |
| 20 | 表示名の制御文字がエラー |
| 21 | 空の表示名でクリアできる |
| 22 | 長すぎる部屋名（31文字）がエラー |
| 23 | 空の部屋名がエラー |
| 24 | 部屋名の上限ちょうど（30文字）は成功 |
| 25 | 部屋名の HTML がエスケープされる |
| 26 | デフォルトワールド削除がエラー |
| 27 | 存在しないワールド削除がエラー |
| 28 | ブックマーク名が長すぎる（21文字）とエラー |
| 29 | ブックマーク名の上限ちょうど（20文字）は成功 |
| 30 | ブックマーク名の制御文字がエラー |
| 31 | ブックマーク51件超過がエラー |
| 32 | ブックマーク50件ちょうどは成功 |
| 33 | 大量配列リクエスト（150要素）がエラーにならない |
| 34 | 不正な range でもエラーにならない（安全フォールバック） |
| 35 | RPC レート制限の設定が存在する |
| 36 | 連続 RPC 呼び出しでレート制限が発動する |
| 37 | レート以内の持続呼び出し（3秒間）は全て成功する |
| 38 | バースト後にトークンが回復する |

---

### `doTest-ping.sh` — サーバ疎通テスト

Nakama サーバに接続し、認証・RPC・Web 配信を確認する。
ローカルでもリモートでも使用可能。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `--host HOST` | 接続先ホスト | `NAKAMA_HOST` or `127.0.0.1` |
| `--port PORT` | Nakama API ポート | `NAKAMA_PORT` or `7350` |
| `--web-port PORT` | Web サーバポート | 自動検出 |

---

### `doTest-ping-remote.sh` — リモート疎通テスト

SSH 経由で VPS 上の Nakama の疎通を確認する。開発環境（WSL2）から実行。

| 引数 | 説明 | デフォルト |
| --- | --- | --- |
| 第1引数 | VPSホスト（必須） | — |
| 第2引数 | SSHユーザー | — |

---

### `doTest-https.sh` — HTTPS デプロイ確認テスト

さくらVPS で HTTPS 設定が正しく動作しているか確認する。
各段階で PASS/FAIL を表示し、どこで問題が発生しているか特定できる。

| 引数 | 説明 | デフォルト |
| --- | --- | --- |
| 第1引数 | ドメイン名（必須） | — |

---

## デプロイテスト

### `doDeployTest.sh` — デプロイテスト

本番デプロイ前にローカル環境（WSL2）で一括テストする。
別ディレクトリに git clone し、ビルド → デプロイ → テストを順に実行する。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `-v` | 全ログ出力 | off |

---

### `doDeployTest-curl.sh` — デプロイテスト（curl版）

任意のディレクトリに配置して実行できる。
curl でリポジトリから doDeployTest.sh を取得して実行する。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `-v` | 全ログ出力 | off |

---

## ユーティリティスクリプト

### `doLint.sh` — 文法チェック

TypeScript, Shell, Go, Docker Compose, YAML の文法チェックを一括実行する。
オプションなし。

### `doSetupTest.sh` — ゼロセットアップ検証

GitHub リポジトリを clone してセットアップ手順を検証する。Docker 起動済みが前提。
オプションなし。

### `doCleanLog.sh` — ログ削除

`test/log/` のタイムスタンプ付きログファイルを削除する。
番号付きシンボリックリンク（00-all.md 等）は残す。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `--all` | シンボリックリンクも含め全削除 | off |

### `doClearGround.sh` — 地面データ初期化

PostgreSQL の storage テーブルから world_data チャンクを全削除する。
削除後はサーバ再起動が必要。オプションなし。

### `doClearUsers.sh` — ユーザーデータ初期化

PostgreSQL から全ユーザーとデバイス認証データを削除する。
削除後はサーバ再起動が必要。オプションなし。

### `doSeedGround.sh` — 初期地面データ投入

OP_BLOCK_UPDATE を使い、テスト用の地面データをサーバに書き込む。

| オプション | 説明 | デフォルト |
| --- | --- | --- |
| `--pattern NAME` | 地面パターン（plaza 等） | plaza |

#### 速度の制約と TODO（未実装、後回し）

サーバ側の `rateLimitBlock=20/sec` ([nakama/go_src/main.go](../nakama/go_src/main.go) `opBlockUpdate` 周辺) に合わせて `seed-ground.test.ts` は 1 ブロックあたり約 56ms スリープしている。plaza (49×49=2,401セル) で約 **2分15秒** 所要。

##### 改善案（未実装）: 管理者ユーザはレート制限をスキップする

- サーバ側修正
  - `opBlockUpdate` / `opChatMsg` / 移動ハンドラのレート制限ブロック直前に「管理者なら skip」分岐を入れる
  - MatchJoin で `authFlagsCache` から `IsAdmin` を取得し、`matchState.IsAdmin[sid]` 等にキャッシュ
- 管理者の指定方法（どちらか）
  - **A案**: 既存の `ADMIN_UIDS` env を流用。seed-ground 用 UID を `.env` に追記してサーバ再起動
  - **B案**: 専用 env `SEED_ADMIN_DEVICE_IDS=seed-ground-admin,...` を新設し、デバイスID で自動 admin 化
- seed-ground テスト修正
  - `await sleep(56)` を削除して数秒で完了させる
- 別解
  - 環境変数 `RATE_LIMIT_BLOCK=10000` で起動して全体を緩める（本番設定とズレる）
  - サーバ停止状態で PostgreSQL の `storage` テーブルに直接 INSERT（チャンク単位 16×16 で一括投入、稼働中は memory cache が更新されないため再起動必須）

##### DB 直挿入時のキャッシュ整合性問題と対策案の検討

`getChunk` ([nakama/go_src/main.go](../nakama/go_src/main.go) L437 周辺) は一度ロードした chunk を `w.loaded[key]=true` でマークし、以降 DB を再読込しない。したがって稼働中に DB を直接書き換えても、サーバメモリ上のチャンクは古いまま、既存接続クライアントには新データが配信されない。

対策案を検討した結果（推奨順）:

- **B案: 管理者 RPC `adminBulkSetGround` を新設（本命）**
  - `{worldId, cx, cz, cells[256]}` を受け取り、メモリ上書き → ハッシュ計算 → markChunkDirty → AOI 内クライアントへ chunk update ブロードキャスト
  - DB 永続化は既存 `flushDirtyChunks` に任せる
  - レート制限は admin 専用としてスキップ
  - **DB 直挿入の必要性そのものを消す**（書き込み経路が一本化され、整合性問題が原理的に発生しない）
  - plaza 49×49 ≒ 10 chunks → 10 RPC で 1秒以内に完了
- **A案: 管理者 RPC `adminReloadChunks`（保険）**
  - `{worldId, chunks:[{cx,cz},...]}`（省略時は全 chunk）を受け取り、`w.mu.Lock()` → `delete(w.Chunks, key)` & `delete(w.loaded, key)`
  - 次回 `getChunk` で DB から再読込される。`calcHash` は再ロード時に走るので追加実装不要
  - B案では救えない「外部ツールで DB を弄った後の整合性復旧」ケース用
- **C案: signal (SIGUSR1) でキャッシュクリア — 不採用**
  - Nakama 本体プロセスが SIGTERM/SIGINT を握っており、プラグインが `signal.Notify` を登録するとシャットダウン処理と競合する恐れ
  - `getChunk` 実行中の `w.Chunks` クリアは競合する。`w.mu.Lock()` で全停止する必要があり、その間 10Hz ティック処理や AOI ブロードキャストもブロック
  - signal 起因の不具合は再現性・観測性が低い。RPC ならログが残る
  - クライアント側キャッシュ (IndexedDB) は signal で消せない。サーバ側 chunk hash が変わらないと `syncChunks` で差分が降りてこないため、いずれにせよハッシュ再計算は必須

結論: **B案を本命、A案を保険として実装**。signal 方式は採用しない。

実装時は本ドキュメントから TODO を削除し、手順を `doSeedGround.sh` の使い方に統合すること。
