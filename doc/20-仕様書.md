# tommieChat仕様書

## オセロ

- クライアント:「7b.オセロ」パネル
  - ゲームロビー
    - テーブルカラム
    - No
      - 整数３桁を表示する。例：001
    - 日時
    - オーナー
      - ゲームのオーナー（作成者）の表示名を表示する。
      - 画面幅が狭いときは表示しない。
    - コメント
      - 左にスクロール
      - 表示する文字列：時分 オーナ表示名 セリフ
        - オーナ表示名は太い文字でオーバレイチャットと同じ色で表示する。
        - セリフ初期値
          - 'がゲーム相手を募集中です！'
      - 閲覧ボタン
        - コメント欄と閲覧ボタンは密着させる。
        - 参加ボタンの左に表示。
      - 参加ボタン
        - 参加ボタンは表の右端に密着させる。
    - ゲーム（ゲーム中、応募中）がないとき
      - オーナー欄を表示しない
      - No欄, 日時欄は空白にする。
      - コメント欄に`*** 新ゲームを開始しよう！ ***`を赤色、太文字で表示する
      - コメント欄を左自動スクロールする
    - BUG
      - オーナーカラムラベル、コメントカラムラベルが重ならないようにする
  - 履歴
    - 黒欄
    - 白欄
    - 黒欄、白欄共通
      - 表示名はオーバレイチャットの表示名と同じ色で表示する。
    - スコア欄
      - 半角6文字分の幅、センタリング
    - 決着欄
      - センタリング
    - 表全体
      - スマホ：ドラッグで縦スクロールに対応
        - カラムラベルはスクロールしない
          - ドラッグしたときにカラムラベルは縦に移動しない。
        - 「履歴」ラベル下部と「No」カラムラベル上部の間に表の値が見切れている。
          - 見きれないようにする。（非表示にする）
          - 表の値のスクロール範囲はカラムラベル下部までにする。
            - カラムラベルと表の値が重ならないようにする。
      - スマホ：ドラッグで横スクロールに対応
        - カラムラベル（No以外）もスクロールする
        - No欄の値はスクロールしない
          - ドラッグしたときに横に移動しない。
  - 日時の更新アルゴリズム
    - １秒ごとに日時を更新する。
      - 時間間隔にはtickを用いる。setIntervalは使わない。
      - 日時の変化があるときだけ、表示項目を更新する。
        - 変化がないときは更新しない（チラツキ防止）
  - No
    - サーバDBが管理するオセロゲーム番号 % 1000 を表示する。3桁
    - ツールチップにはゲーム番号そのママ表示する。
  - ゲームロビー、履歴の表の幅はパネル幅にする
  - パネルリサイズでサーバへRPC、websocket通知が発生しないこと。

  - 「対戦相手を待っています」パネル
    - オセロゲーム番号を画面左上に表示する
      - 例："ゲーム番号:012"

- サーバ

- 全体
  - index.htmlページタイトルは"tommieChat "+ バージョン + "#" + コミット番号
- Playwright
  - スクリーンショットは test/ss/ に保存する。

- 通知の案
  - 想定している使い方
    - オセロゲーム
      - 1.新ゲーム開始ボタンでゲーム作成→パネルを閉じる
      - 2.対戦相手が参加ボタンを押した
      - 3.ゲームオーナーへ通知が来る
        - 通知「対戦相手が見つかりました。[オセロへ移動するリンク]」
          - リンクURL: `?ot=123`
            - othello(オセロパネル指定)
            - オセロゲーム番号
              - （想定している場面）対戦相手待ちのゲーム番号のとき、オセロゲーム開始
              - ゲーム中の番号のとき、オセロ閲覧表示
              - それ以外の番号のとき、オセロパネルトップ表示
                - 存在しない番号 / 削除済み / 既に満員 / 相手が離脱後
                - 理由をトースト表示する
                  - 例：「オセロゲーム番号123は相手がオフラインです」
            - リンクURLは一般的に使う想定
              - QRコードに`https://mmo.tommie.jp/?ot=123`を埋め込む
                - ユーザAがtommieChatでQRコードを表示
                - ユーザBがQRコードアプリで読み込み、URLを開くとオセロ対戦画面へ
  - 表示方法
    - 詳細は トースト（吹き出し） セクション参照
  - 通知フォーマット（サーバ→クライアント）
    - subject: "対戦相手が見つかりました"（固定）
    - content:
      - gameNo: int 例: 123
      - opponentName: string 例: "ユーザB"
    - クライアントでの吹き出し文字列
      - `{opponentName}が見つかりました。ゲーム番号:{gameNo:3桁}`
      - タップ → `?ot={gameNo}`
  - 実装手順
    - ✔1.クライアント：リンク対応?ot=123
      -オセロパネルへ移動
      -ブラウザのURLでテスト
      -詳細はURLスキーム規約を参照
    -☓2.不要：サーバ：オセロイベントリスナー登録(websocket)を受信できるようにする。
    -☓3.不要：クライアント：オセロゲーム作成時、オセロイベントリスナー登録(wsocket)をサーバへ送信
    -☓4.不要：クライアント：対戦相手が参加ボタン→オセロ参加イベント通知(websocket)をサーバへ送信
    -✔4.1サーバ：既に実装済みの参加ボタンの処理に以下を追加
        -nakamaサーバのsocket.notificationsでオーナーへ参加通知送信
        -DB永続化する。
        -具体的なソース位置
            nakama/go_src/main.go:4397-4449 rpcOthelloJoin()
    -✔5.クライアント：参加通知を受信したら、吹き出し通知表示
      -socket.onnotificationsを用いる。
    -✔6.クライアント：吹き出し通知タップでリンク先アクセス→オセロパネル表示
      -既にオセロパネル（対戦相手を待ってます表示中）なら吹き出し不要。
        -マッチメッセージ経由で自動的に「対戦相手を待ってます → 対局開始」へ画面遷移(既に実装済み)
      -URL反映：history.pushState で URL を `?ot={gameNo}` に更新
        -ブラウザ「戻る」で元の画面に戻れる

-URLスキーム規約
    -?ot=123 オセロパネル表示、オセロゲーム番号123を表示
    -?panel=othelloはサポートしない
    -v2以後
        -?sg=123 将棋パネル表示
        -?dn 表示名設定パネル表示
            -?dn=name1 不採用
                -不採用理由：URLでパネル指定はOK、値の書き込みは不採用
                -パネルだけ開く。値は無視
        -?as アバター選択パネル表示
            -?as=123 アバター番号123をアバター選択パネルで表示
        -?rm=123 部屋番号123へ移動
            -v3:`https://mmo.tommie.jp/rm/123`123は部屋番号
            -v3:`https://mmo.tommie.jp/rm123` rm123は部屋名
        -?pl=512,513 座標(512,513)へ移動
        -?pl=場所名1 場所名1へ移動
        -?rm=123&pl=512,513 部屋番号123の座標(512,513)へ移動

    -別サーバ移動（URL全体）
        -`https://mmo.sample.jp` 別サーバへ移動
            -外部サーバに誘導できるとフィッシングリスク。
            -既知サーバのホワイトリストに含まれるドメインのみ許可
            -未知ドメインは「このURLは外部サーバです。移動しますか？」確認ダイアログ

    -QRコード対応
        -`https://mmo.tommie.jp/?ot=123`を埋め込む
        -QRリンク?ot=123をログインしてない状態で開いたとき
            -未ログイン（デバイス認証）でもオセロゲームはできる
            -名前がユーザID（デバイス認証のID:例3a8f)が読みにくいがゲームはできる
                -v1:これは仕様として許容する（誰でもすぐ対戦できるのが利点）
                -v2:表示名設定ダイアログを最初に表示し、設定後、オセロパネルへ自動で移動
        -v1:PWAはサポートしない
            -iOSではQRコードからPWAを開けない
                -safariが開く。
            -safari, PWAでデバイスIDが別になるが、safariとPWAでgoogle認証しておけば解決するので、google認証を推奨する。
            -google認証は既に実装済み。
            -デバイス認証でのiOS safari, PWAが別ユーザ扱いになる件はサポートしない。（google認証推奨）
        -v2:PWAをフルサポートするのはAndroid,PCのみ。iOSでは制限あり。
            -PWAは少数派。
-URLパラメータ読み取りのタイミング（遅延呼び出し）
    -QR から入ってきた ?ot=123 の処理順序:
        -1.URLパラメータ(?ot=123)をURL_parameter変数へ覚えておく
        -2.ページロード
        -3.authenticateDevice (localStorage or cookie → deviceId)
        -4.socket 接続確立
        -5. 1で設定した変数にパラメータがあるなら処理する
            -URLパラメータを読み、オセロパネルを開く
            -対象 gameNo の状態を RPC で確認 (待機中 / 対局中 / 不在)
            -不在ならトースト表示してパネルトップ
        -メモ
            -URL_parameter変数はメモリ保持のみ（localStorage 退避不要）
            -ページロード完了→socket接続確立、までの遅延を吸収する目的

-socket notification DB永続化(persistent=true) の運用
    -起動時の取り残し回収
        -クライアント起動時（socket接続確立後）にclient.listNotifications(session, 100) で取り残しを取得
        -処理 → deleteNotifications で消化
    -受信ハンドラでも処理後 deleteNotifications を呼ぶ（重複排除）
    -意図：DBに通知が溜まり続ける＆起動のたびに全件再受信を防止

-トースト（吹き出し）
    -画面右下端（送信ボタンの少し上あたり）に吹き出し💬のような背景で、通知文字列を表示
    -ソフトキーボード表示中も送信ボタンに追従し相対位置（上）に表示する
        -実装: window.visualViewport の resize イベントで追従
        -対応: iOS Safari 13+, Android Chrome
    -YES/NOタイプ
    -ユーザA:フレンド申請→ユーザBの吹き出し:「Aさんからフレンド申請が来ました。受け付けますか？[YES] [NO]」
        -YES フレンド申請を受け付ける。フレンドになる。
        -NO 拒否。
        -クライアントが受信したとき、socket.notificationの種類で判定
            -CodeFriendRequest：yes, noボタンを吹き出し中に表示
            -それ以外：yes, noボタンを表示しない
    -トーストUIは汎用コンポーネント化
    -短い文章１行を想定、Yes/Noボタンを含めると２行になるかも
    -✔受信したらピっと音がする
        -音:ブラウザ自動再生ポリシーで、初回ユーザジェスチャ前は鳴らない → ログイン操作時に無音再生して解禁が定番
            -初回 silent 再生解禁のタイミング: ログインボタン押下時
        -ファイル：public/sounds/notification.mp3（ビルド時に /sounds/notification.mp3 で配信）
        -音量: 0.5 程度
        -ユーザON/OFF: デバッグパネル 02b 近辺に 通知設定 セクション追加
    -v1:可能なら振動（可能な環境のみ）
            -navigator.vibrate(100) で 1 行実装、未対応環境は自動 no-op
            -navigator.vibrate()はiOS Safari(非PWA)では効かない
    -時間が経過すると消える：10秒
    -フレンド申請、DM、いいね！（された側に表示）などにも使う予定
    -v2:複数同時到着したときは古いものが上へずれる。
        -最大5件表示
        -ドラッグで過去の吹き出しをスクロール

-socket.notification 定数名

| Code | 定数名 | 用途 | Yes/No |
| --- | --- | --- | --- |
| 1001 | CodeOthelloJoined | オセロ参加 | No |
| 1002 | CodeDirectMessage | DM | No |
| 1003 | CodeLiked | いいね | No |
| 1004 | CodeFriendRequest | フレンド申請 | Yes |

-✔FIX BUG:スクロールバーに伴う青いライン（ゴミ）

    ```text
    FIX BUG:スクロールバーに伴う青いライン（ゴミ）
    画面左下にある青い■が３つある（ゴミ）がでる。
    履歴のスクロールバーを動かすとゴミも動く。
    これは何度かでているバグで、スクロールバーが原因。
    スクロールバーと３D領域が干渉していることが原因。
    直して。
    原因は Chromium の既知バグ：position: sticky のセルが overflow: auto のスクロールコンテナ内にあり、その祖先に transform が効いた position: fixed があると、スクロール時にスティッキーセルがビューポート原点に漏れて描画される問題です。
contain: paint を履歴とゲーム一覧のスクロール要素に付与して、描画範囲を要素内に閉じ込めます。
    ```

- ✔FIX BUG: iPhone safariで音がならない。
  - 状況：iPadMini, windows11 chromeではオセロ駒を置いたときなど音がする。
  - 原因：iOS Safari は `AudioContext` を user gesture 内で `resume()` しないとサスペンド状態のまま無音になる。iPad は挙動が緩いので問題化しにくい。
  - さらに本アプリは自動ログイン経路（`setTimeout(doLogin, 100)`）が存在するため、`doLogin` 冒頭で `unlockAudio()` を呼んでも gesture 外となり解禁に失敗する。
  - 対処：
    - 共有 `AudioContext` を `UIPanel` 初期化時に用意し、`unlockAudio()` で `resume()` + 無音バッファ再生。
    - `document` に `pointerdown` / `keydown` / `touchstart` の capture リスナーを仕込み、最初の user gesture で `unlockAudio()` を必ず実行（成功したら解除）。
    - `doLogin` 冒頭でも `unlockAudio()` を呼ぶ（ユーザ操作ログイン時の保険）。
    - `othelloPlaySound` は共有 ctx を使い、呼び出し時に suspended なら `resume()` を試みる。
  - 追加対処（HTMLAudioElement 併用）：
    - `unlockAudio()` 内で無音 WAV data URI を `new Audio()` で読み込み `loop=true`・`volume=0`・`playsInline=true` で `.play()` 実行。
    - iOS Safari は HTMLAudioElement を一度でも再生すると audio session が確立し、以降の WebAudio 再生が通りやすくなる。
    -テスト結果：鳴った。

-✔FIX BUG:トースト音がしない。
    src/Toast.ts:34-62 ensureNotifAudio() + primeNotificationSound() を追加（user gesture 内で play().pause() して iOS Safari の Audio 要素を解禁）
    src/UIPanel.ts:8 primeNotificationSound を import
    src/UIPanel.ts:2679-2680 unlockAudio() から primeNotificationSound() を呼び出し
    根本原因: iOS Safari は Audio 要素ごとに user gesture での解禁が必要。これまでは silent WAV 用の別 Audio 要素しか解禁しておらず、通知音 mp3 の Audio 要素が未解禁のまま非 gesture 経路（websocket 通知）で再生しようとして autoplay ブロックされていた。最初のタップで mp3 の方も play()→pause() で解禁するよう修正。
    -テスト結果（1回目）：iPadMiniで鳴らない。iPhone safari, windows Chromeは鳴る。iPadMiniでは他のイベント(駒を置くなど)は鳴っている。
    -追加対処：HTMLAudioElement から WebAudio の `decodeAudioData` + `BufferSourceNode` 経路に切替（旧 iPad Safari は HTMLAudio の非 gesture 再生が不安定なため）。
        -src/Toast.ts で `notifAudioCtx`・`notifBuffer` を用意し、`primeNotificationSound()` で fetch → decode → buffer 保持。
        -再生時は `BufferSourceNode` を都度生成（同時多発再生でも重ならず独立再生）。WebAudio 失敗時は HTMLAudio にフォールバック。
            -テスト結果：iPhone safari, iPadMini, Windows11 chromeでOK

-✔FIX BUG:iPhone safari, オセロのゲーム画面がパネルをリサイズすると、くずれる

    修正しました。UIPanel.ts:4609 の fitBoard() を以下のように変更しました:

    絶対配置の子要素を除外 — #othello-game-no-label (position: absolute) が usedH と visCount に含まれていた → getComputedStyle(el).position === "absolute" でスキップ。これで盤面高さが約14px 過剰に削られる + gap 1回分(4px) 多くカウントされる問題を解消。
    rAF でレイアウト確定を待つ — iPhone Safari は visualViewport 遷移中に getBoundingClientRect() を呼ぶと旧値を返すことがあるため、requestAnimationFrame で次フレームに測定を遅延。
    iPhone Safari で試して、パネルリサイズ時にオセロ盤面が崩れなくなったか確認してください

        → テスト結果：直っていない。全く同じ。

    追加の修正を入れました。原因は CSS 側に「盤面が縮まないためのあふれ」が発生していた可能性が高いです。

    変更:

    index.html:1277-1291 #othello-board — flex-shrink: 0 を flex-shrink: 1 に変更し、max-width: 100%; max-height: 100%; min-width: 0; min-height: 0 を追加。これで JS の fitBoard がわずかに大きいサイズを渡しても、盤面がコンテナを超えてはみ出さない。

    index.html:1288-1297 .othello-cell — aspect-ratio: 1 を削除（盤面自体が正方形になっているので不要）。iOS Safari でグリッド（1fr 行）と aspect-ratio が競合する可能性を回避。

    src/UIPanel.ts:4609-4646 fitBoard — 前回の修正（絶対配置要素を除外、rAF で測定遅延）はそのまま維持。

    これで、たとえ JS 側の計算が1フレーム遅れても CSS 側でクランプされるため、panel リサイズ中も盤面が崩れなくなるはずです。iPhone Safari で試してみてください。

        →テスト結果：OK

- ✔PlantUML:状態遷移図
  - 作成: [doc/57-state-othello.puml](57-state-othello.puml)
  - レンダリング: [doc/57-state-othello.png](57-state-othello.png)
  - 状態: ロビー / 待ち受け / ゲームプレイ / 観戦 / 結果
  - 主要遷移:
    - ロビー → 待ち受け（新ゲーム開始）/ プレイ（参加）/ 観戦（閲覧）
    - 待ち受け → プレイ（相手参加通知）/ ロビー（自ゲーム削除）
    - プレイ → 結果（投了 / 両者置けない / 満杯）
    - 観戦 → 結果（対局終了）/ ロビー
    - 結果 → ロビー
  - URL `?ot=gameNo` 直接参入:
    - 自分の待機中ゲーム → 待ち受け
    - 自分の対局中ゲーム → プレイ
    - 他人の対局中 → 観戦
    - 不在/満員 → ロビー（理由トースト）

✔FIX BUG：以下のテストで画面がG２へ行ってしまう。
    正解：G3へ行く
    A: iPhone safari
    B: Windows11 chrome
    １．A：G1→E7:新ゲーム作成ボタン→G2→E18→END
    ２．B：G1→参加ボタン（A:E10）
    BUG:   ３．A：トースト→●→E2→G6→E3→G2
    正解：３．A：トースト→●→E2→G6→E3→G3

    Nakama 統合テスト群は環境起因の失敗（socket 未接続）で、私の変更とは無関係です。型チェックは通過しています。

    修正内容
    バグの原因: A がパネル閉じた状態で currentGameId と gameStatus="waiting" が保持されたまま、B の参加後にトーストでパネルを開き直すと、list 受信ハンドラの if (!currentGameId && ...) ガードで applyGameList が呼ばれず、UI が G2 のまま固着。

    src/UIPanel.ts に 3点の修正:

    list ハンドラのガード撤廃 (onOthelloUpdate): applyGameList は常に実行。状態遷移検知のため。
    applyGameList の構造変更: 自動復帰検知を先頭（常時実行）に分離し、テーブル描画はロビー表示中のみ実行。状態が変わらないときは再描画をスキップ（stateChanged ガード）。
    own game 対応: othelloJoin(gameId, true) に変更。watch=false だとサーバが "cannot join own game" で拒否し盤面状態を取得できない問題を修正。
    これで A: トースト→E2→G6→E3→G3 の流れが正しく動作します。

        →⭐️テスト結果：OK
